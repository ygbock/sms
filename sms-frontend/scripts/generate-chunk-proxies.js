const fs = require(fs);
const path = require(path);
const chunksDir = path.resolve(__dirname, .., .next, server, chunks);
const outDir = path.resolve(__dirname, .., .next, server);
if (!fs.existsSync(chunksDir)) {
  console.error(chunks
